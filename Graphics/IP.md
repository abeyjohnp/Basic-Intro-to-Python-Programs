Image Processing
If we provide a file name it will reutrn a file object
i.getiwdth to get thw idth of the image, height of the image
i.getpixel() given a xy point, get the rgb vales, it will return a tuple
at xy the colour, the given rgb wvalue will be given to that pixle
i.draw() draw that img to canvas
i.clone() to create a copy
i.save() to save the image, i.save(Filename) that file will be saved in that filename

this is the image module
only basic mathematical stuff we are doing here
